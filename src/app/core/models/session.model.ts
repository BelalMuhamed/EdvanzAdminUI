/**
 * Lean session projection for select/dropdown controls.
 * Mirrors Edvanz.Application.Dtos.Session.SessionLookupItemDto exactly.
 */
export interface SessionLookupItem {
  id: number;
  sessionName: string;
}
